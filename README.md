A wrapper for the F# compiler `fsc`
===================================

Are you missing being able to run the F# compiler `fsc` (aka
`fsharpc`) after moving to `dotnet` from `mono`?  This project is for
you.
